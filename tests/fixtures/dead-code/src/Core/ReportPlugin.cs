namespace DeadCode.Core
{
    /// <summary>Loaded by name from configuration: nothing references it in code.</summary>
    public sealed class ReportPlugin
    {
        public string Name
        {
            get { return "reports"; }
        }
    }
}
